@Override
public void displayDetails() {
    System.out.println("Details");
}

// Here, `@Override` is an annotation that tells the Java compiler that the method is intended to override a method in a superclass.